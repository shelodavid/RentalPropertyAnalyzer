# UI Overhaul Foundation

This branch establishes the baseline UI conventions (layout structure, shared partials, spacing/typography rules)
used for subsequent UI work.

No business logic changes intended.
